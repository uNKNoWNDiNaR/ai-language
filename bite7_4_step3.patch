--- /frontend/src/api/lessonAPI.ts	2026-02-04 20:44:06.000000000 +0000
+++ /frontend/src/api/lessonAPI.ts	2026-02-05 04:19:12.446461952 +0000
@@ -163,6 +163,50 @@
   return data;
 }
 
+export type SuggestedReviewItem = {
+  conceptTag: string;
+  lastSeenAt: string;
+  mistakeCount: number;
+  score: number;
+};
+
+export type SuggestedReviewResponse = {
+  items: SuggestedReviewItem[];
+};
+
+export async function getSuggestedReview(params: {
+  userId: string;
+  language: SupportedLanguage;
+  limit?: number;
+}): Promise<SuggestedReviewResponse> {
+  const { data } = await http.get<SuggestedReviewResponse>("/review/suggested", { params });
+  return data;
+}
+
+export type GeneratePracticeResponse = {
+  practiceItem: {
+    practiceId: string;
+    prompt: string;
+  };
+  source?: {
+    questionId?: string;
+    conceptTag?: string;
+    reason?: string;
+  };
+};
+
+export async function generatePractice(params: {
+  userId: string;
+  language: SupportedLanguage;
+  lessonId: string;
+  questionId?: string;
+  conceptTag?: string;
+  type?: "variation";
+}): Promise<GeneratePracticeResponse> {
+  const { data } = await http.post<GeneratePracticeResponse>("/practice/generate", params);
+  return data;
+}
+
 export async function submitFeedback(params:FeedbackRequest): Promise<FeedbackResponse> {
   const { data } = await http.post<FeedbackResponse>("/feedback", params);
   return data;
--- /frontend/src/components/Lesson.tsx	2026-02-04 20:44:06.000000000 +0000
+++ /frontend/src/components/Lesson.tsx	2026-02-05 04:31:45.964825619 +0000
@@ -7,6 +7,8 @@
   submitAnswer,
   getSession,
   submitPractice,
+  generatePractice,
+  getSuggestedReview,
   toUserSafeErrorMessage,
   getHttpStatus,
   type LessonSession,
@@ -17,6 +19,7 @@
   type TeachingPace,
   type ExplanationDepth,
   type TeachingPrefs,
+  type SuggestedReviewItem,
 } from "../api/lessonAPI";
 
 type Role = "user" | "assistant";
@@ -194,6 +197,13 @@
   const [explanationDepth, setExplanationDepth] = useState<ExplanationDepth>("normal");
   const [hintAnchorIndex, setHintAnchorIndex] = useState<number | null>(null);
 
+  const [suggestedReviewItems, setSuggestedReviewItems] = useState<SuggestedReviewItem[]>([]);
+  const [reviewDismissed, setReviewDismissed] = useState(false);
+  const [reviewQueue, setReviewQueue] = useState<SuggestedReviewItem[]>([]);
+  const [reviewIndex, setReviewIndex] = useState(0);
+  const [reviewBanner, setReviewBanner] = useState<string | null>(null);
+  const [practiceMode, setPracticeMode] = useState<null | "lesson" | "review">(null);
+
 
 
   const [lastSessionKey, setLastSessionKey] = useState<string>(() => {
@@ -332,6 +342,79 @@
     if (lessonCompleted) setMoreOpen(false);
   }, [lessonCompleted]);
 
+
+  async function refreshSuggestedReview(nextSession: LessonSession) {
+    try {
+      const res = await getSuggestedReview({
+        userId: nextSession.userId,
+        language: nextSession.language,
+        limit: 2,
+      });
+
+      const items = Array.isArray(res.items) ? res.items : [];
+      setSuggestedReviewItems(items);
+      setReviewDismissed(false);
+    } catch {
+      setSuggestedReviewItems([]);
+    }
+  }
+
+  function stopReview(message?: string) {
+    setPracticeId(null);
+    setPracticePrompt(null);
+    setPracticeAnswer("");
+    setPracticeTutorMessage(null);
+    setPracticeAttemptCount(null);
+
+    setPracticeMode(null);
+    setReviewQueue([]);
+    setReviewIndex(0);
+
+    if (message) setReviewBanner(message);
+  }
+
+  async function loadReviewPractice(item: SuggestedReviewItem) {
+    if (!session) return;
+
+    setReviewBanner(null);
+    setError(null);
+    setLoading(true);
+
+    try {
+      const res = await generatePractice({
+        userId: session.userId,
+        language: session.language,
+        lessonId: session.lessonId,
+        conceptTag: item.conceptTag,
+        type: "variation",
+      });
+
+      const pi = res.practiceItem;
+      if (!pi?.practiceId || !pi?.prompt) throw new Error("Invalid practice item");
+
+      setPracticeId(pi.practiceId);
+      setPracticePrompt(pi.prompt);
+      setPracticeAnswer("");
+      setPracticeTutorMessage(null);
+      setPracticeAttemptCount(null);
+      setPracticeMode("review");
+    } catch {
+      stopReview("Couldn't load review right now. You can continue the lesson.");
+    } finally {
+      setLoading(false);
+    }
+  }
+
+  async function beginSuggestedReview() {
+    if (!session || practiceActive || lessonCompleted) return;
+    if (suggestedReviewItems.length === 0) return;
+
+    setReviewDismissed(true);
+    setReviewQueue(suggestedReviewItems);
+    setReviewIndex(0);
+    await loadReviewPractice(suggestedReviewItems[0]);
+  }
+
   async function handleStart() {
     setMoreOpen(false);
     setError(null);
@@ -347,6 +430,13 @@
     setPracticeTutorMessage(null);
     setPracticeAttemptCount(null);
 
+    setSuggestedReviewItems([]);
+    setReviewDismissed(false);
+    setReviewQueue([]);
+    setReviewIndex(0);
+    setReviewBanner(null);
+    setPracticeMode(null);
+
     setLoading(true);
     setPending("start");
 
@@ -377,6 +467,8 @@
       setPracticeAnswer("");
       setPracticeTutorMessage(null);
       setPracticeAttemptCount(null);
+      setPracticeMode(null);
+      void refreshSuggestedReview(res.session);
     } catch (e: unknown) {
       setError(toUserSafeErrorMessage(e));
     } finally {
@@ -408,6 +500,8 @@
       } catch {
         // ignore
       }
+
+      void refreshSuggestedReview(res.session);
     } catch (e: unknown) {
       const status = getHttpStatus(e);
       if (status === 404) {
@@ -448,6 +542,13 @@
     setPracticeTutorMessage(null);
     setPracticeAttemptCount(null);
 
+    setSuggestedReviewItems([]);
+    setReviewDismissed(false);
+    setReviewQueue([]);
+    setReviewIndex(0);
+    setReviewBanner(null);
+    setPracticeMode(null);
+
     setLoading(true);
     setPending("start");
 
@@ -478,6 +579,8 @@
       setPracticeAnswer("");
       setPracticeTutorMessage(null);
       setPracticeAttemptCount(null);
+      setPracticeMode(null);
+      void refreshSuggestedReview(res.session);
     } catch (e: unknown) {
       setError(toUserSafeErrorMessage(e));
     } finally {
@@ -503,6 +606,13 @@
     setPracticeTutorMessage(null);
     setPracticeAttemptCount(null);
 
+    setSuggestedReviewItems([]);
+    setReviewDismissed(false);
+    setReviewQueue([]);
+    setReviewIndex(0);
+    setReviewBanner(null);
+    setPracticeMode(null);
+
     setPending(null);
     setMoreOpen(false);
   }
@@ -586,6 +696,7 @@
         setPracticeAnswer("");
         setPracticeTutorMessage(null);
         setPracticeAttemptCount(null);
+        setPracticeMode("lesson");
       } else if (res.hint?.level === 3) {
         //ensure practice doesnt appear after reveal
         setPracticeId(null);
@@ -593,6 +704,7 @@
         setPracticeAnswer("");
         setPracticeTutorMessage(null);
         setPracticeAttemptCount(null);
+        setPracticeMode(null);
       }
 
       setProgress(res.progress ?? null);
@@ -623,9 +735,25 @@
       setPracticeAttemptCount(res.attemptCount);
 
       if (res.result === "correct") {
-        setPracticeId(null);
-        setPracticePrompt(null);
-        setPracticeAnswer("");
+        if (practiceMode === "review") {
+          const nextIdx = reviewIndex + 1;
+          if (nextIdx < reviewQueue.length) {
+            setReviewIndex(nextIdx);
+            setPracticeId(null);
+            setPracticePrompt(null);
+            setPracticeAnswer("");
+            setPracticeTutorMessage(null);
+            setPracticeAttemptCount(null);
+            await loadReviewPractice(reviewQueue[nextIdx]);
+          } else {
+            stopReview("Review complete. Continue when you're ready.");
+          }
+        } else {
+          setPracticeId(null);
+          setPracticePrompt(null);
+          setPracticeAnswer("");
+          setPracticeMode(null);
+        }
       } else {
         setPracticeAnswer("");
       }
@@ -1003,6 +1131,498 @@
         </div>
       )}
 
+
+
+      {reviewBanner && (
+        <div
+          style={{
+            marginBottom: 12,
+            padding: 12,
+            borderRadius: 16,
+            border: "1px solid #e6e6e6",
+            background: "white",
+            boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+          }}
+        >
+          <div style={{ fontSize: 13, opacity: 0.85 }}>{reviewBanner}</div>
+        </div>
+      )}
+
+      {session &&
+        !lessonCompleted &&
+        !practiceActive &&
+        suggestedReviewItems.length > 0 &&
+        !reviewDismissed && (
+          <div
+            style={{
+              marginBottom: 12,
+              padding: 12,
+              borderRadius: 16,
+              border: "1px solid #e6e6e6",
+              background: "white",
+              boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+            }}
+          >
+            <div
+              style={{
+                display: "flex",
+                justifyContent: "space-between",
+                alignItems: "center",
+                marginBottom: 8,
+              }}
+            >
+              <div style={{ fontSize: 12, opacity: 0.75 }}>Suggested review</div>
+              <div style={{ fontSize: 12, opacity: 0.7 }}>Optional</div>
+            </div>
+
+            <div style={{ fontSize: 13, opacity: 0.85, marginBottom: 10 }}>
+              Want to review {Math.min(2, suggestedReviewItems.length)} item
+              {Math.min(2, suggestedReviewItems.length) === 1 ? "" : "s"} you struggled with last time?
+            </div>
+
+            <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 12 }}>
+              {suggestedReviewItems.slice(0, 2).map((it) => (
+                <div
+                  key={it.conceptTag}
+                  style={{
+                    padding: "6px 10px",
+                    borderRadius: 999,
+                    background: "#F2F2F7",
+                    border: "1px solid #e6e6e6",
+                    fontSize: 12,
+                    opacity: 0.92,
+                  }}
+                >
+                  {it.conceptTag.replace(/_/g, " ")}
+                </div>
+              ))}
+            </div>
+
+            <div style={{ display: "flex", gap: 10 }}>
+              <button
+                onClick={() => void beginSuggestedReview()}
+                disabled={loading}
+                style={{
+                  padding: "10px 14px",
+                  borderRadius: 12,
+                  border: "1px solid",
+                  borderColor: loading ? "#ddd" : "#007AFF",
+                  background: loading ? "#E5E5EA" : "#007AFF",
+                  color: loading ? "#666" : "white",
+                  cursor: loading ? "not-allowed" : "pointer",
+                }}
+              >
+                Review now
+              </button>
+              <button
+                onClick={() => setReviewDismissed(true)}
+                disabled={loading}
+                style={{
+                  padding: "10px 14px",
+                  borderRadius: 12,
+                  border: "1px solid #ddd",
+                  background: "white",
+                  cursor: loading ? "not-allowed" : "pointer",
+                  opacity: loading ? 0.7 : 1,
+                }}
+              >
+                Not now
+              </button>
+            </div>
+          </div>
+        )}
+
+      {reviewBanner && (
+        <div
+          style={{
+            marginBottom: 12,
+            padding: 12,
+            borderRadius: 16,
+            border: "1px solid #e6e6e6",
+            background: "white",
+            boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+          }}
+        >
+          <div style={{ fontSize: 13, opacity: 0.85 }}>{reviewBanner}</div>
+        </div>
+      )}
+
+      {session &&
+        !lessonCompleted &&
+        !practiceActive &&
+        suggestedReviewItems.length > 0 &&
+        !reviewDismissed && (
+          <div
+            style={{
+              marginBottom: 12,
+              padding: 12,
+              borderRadius: 16,
+              border: "1px solid #e6e6e6",
+              background: "white",
+              boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+            }}
+          >
+            <div
+              style={{
+                display: "flex",
+                justifyContent: "space-between",
+                alignItems: "center",
+                marginBottom: 8,
+              }}
+            >
+              <div style={{ fontSize: 12, opacity: 0.75 }}>Suggested review</div>
+              <div style={{ fontSize: 12, opacity: 0.7 }}>Optional</div>
+            </div>
+
+            <div style={{ fontSize: 13, opacity: 0.85, marginBottom: 10 }}>
+              Want to review {Math.min(2, suggestedReviewItems.length)} item
+              {Math.min(2, suggestedReviewItems.length) === 1 ? "" : "s"} you
+              struggled with last time?
+            </div>
+
+            <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 12 }}>
+              {suggestedReviewItems.slice(0, 2).map((it) => (
+                <div
+                  key={it.conceptTag}
+                  style={{
+                    padding: "6px 10px",
+                    borderRadius: 999,
+                    background: "#F2F2F7",
+                    border: "1px solid #e6e6e6",
+                    fontSize: 12,
+                    opacity: 0.92,
+                  }}
+                >
+                  {it.conceptTag.replace(/_/g, " ")}
+                </div>
+              ))}
+            </div>
+
+            <div style={{ display: "flex", gap: 10 }}>
+              <button
+                onClick={() => void beginSuggestedReview()}
+                disabled={loading}
+                style={{
+                  padding: "10px 14px",
+                  borderRadius: 12,
+                  border: "1px solid",
+                  borderColor: loading ? "#ddd" : "#007AFF",
+                  background: loading ? "#E5E5EA" : "#007AFF",
+                  color: loading ? "#666" : "white",
+                  cursor: loading ? "not-allowed" : "pointer",
+                }}
+              >
+                Review now
+              </button>
+              <button
+                onClick={() => setReviewDismissed(true)}
+                disabled={loading}
+                style={{
+                  padding: "10px 14px",
+                  borderRadius: 12,
+                  border: "1px solid #ddd",
+                  background: "white",
+                  cursor: loading ? "not-allowed" : "pointer",
+                  opacity: loading ? 0.7 : 1,
+                }}
+              >
+                Not now
+              </button>
+            </div>
+          </div>
+        )}
+
+      {reviewBanner && (
+        <div
+          style={{
+            marginBottom: 12,
+            padding: 12,
+            borderRadius: 16,
+            border: "1px solid #e6e6e6",
+            background: "white",
+            boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+          }}
+        >
+          <div style={{ fontSize: 13, opacity: 0.85 }}>{reviewBanner}</div>
+        </div>
+      )}
+
+      {session &&
+        !lessonCompleted &&
+        !practiceActive &&
+        suggestedReviewItems.length > 0 &&
+        !reviewDismissed && (
+          <div
+            style={{
+              marginBottom: 12,
+              padding: 12,
+              borderRadius: 16,
+              border: "1px solid #e6e6e6",
+              background: "white",
+              boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+            }}
+          >
+            <div
+              style={{
+                display: "flex",
+                justifyContent: "space-between",
+                alignItems: "center",
+                marginBottom: 8,
+              }}
+            >
+              <div style={{ fontSize: 12, opacity: 0.75 }}>Suggested review</div>
+              <div style={{ fontSize: 12, opacity: 0.7 }}>Optional</div>
+            </div>
+
+            <div style={{ fontSize: 13, opacity: 0.85, marginBottom: 10 }}>
+              Want to review {Math.min(2, suggestedReviewItems.length)} item
+              {Math.min(2, suggestedReviewItems.length) === 1 ? "" : "s"} you struggled with last time?
+            </div>
+
+            <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 12 }}>
+              {suggestedReviewItems.slice(0, 2).map((it) => (
+                <div
+                  key={it.conceptTag}
+                  style={{
+                    padding: "6px 10px",
+                    borderRadius: 999,
+                    background: "#F2F2F7",
+                    border: "1px solid #e6e6e6",
+                    fontSize: 12,
+                    opacity: 0.92,
+                  }}
+                >
+                  {it.conceptTag.replace(/_/g, " ")}
+                </div>
+              ))}
+            </div>
+
+            <div style={{ display: "flex", gap: 10 }}>
+              <button
+                onClick={() => void beginSuggestedReview()}
+                disabled={loading}
+                style={{
+                  padding: "10px 14px",
+                  borderRadius: 12,
+                  border: "1px solid",
+                  borderColor: loading ? "#ddd" : "#007AFF",
+                  background: loading ? "#E5E5EA" : "#007AFF",
+                  color: loading ? "#666" : "white",
+                  cursor: loading ? "not-allowed" : "pointer",
+                }}
+              >
+                Review now
+              </button>
+              <button
+                onClick={() => setReviewDismissed(true)}
+                disabled={loading}
+                style={{
+                  padding: "10px 14px",
+                  borderRadius: 12,
+                  border: "1px solid #ddd",
+                  background: "white",
+                  cursor: loading ? "not-allowed" : "pointer",
+                  opacity: loading ? 0.7 : 1,
+                }}
+              >
+                Not now
+              </button>
+            </div>
+          </div>
+        )}
+      {reviewBanner && (
+        <div
+          style={{
+            marginBottom: 12,
+            padding: 12,
+            borderRadius: 16,
+            border: "1px solid #e6e6e6",
+            background: "white",
+            boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+          }}
+        >
+          <div style={{ fontSize: 13, opacity: 0.85 }}>{reviewBanner}</div>
+        </div>
+      )}
+
+      {session &&
+        !lessonCompleted &&
+        !practiceActive &&
+        suggestedReviewItems.length > 0 &&
+        !reviewDismissed && (
+          <div
+            style={{
+              marginBottom: 12,
+              padding: 12,
+              borderRadius: 16,
+              border: "1px solid #e6e6e6",
+              background: "white",
+              boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+            }}
+          >
+            <div
+              style={{
+                display: "flex",
+                justifyContent: "space-between",
+                alignItems: "center",
+                marginBottom: 8,
+              }}
+            >
+              <div style={{ fontSize: 12, opacity: 0.75 }}>Suggested review</div>
+              <div style={{ fontSize: 12, opacity: 0.7 }}>Optional</div>
+            </div>
+
+            <div style={{ fontSize: 13, opacity: 0.85, marginBottom: 10 }}>
+              Want to review {Math.min(2, suggestedReviewItems.length)} item
+              {Math.min(2, suggestedReviewItems.length) === 1 ? "" : "s"} you struggled with last time?
+            </div>
+
+            <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 12 }}>
+              {suggestedReviewItems.slice(0, 2).map((it) => (
+                <div
+                  key={it.conceptTag}
+                  style={{
+                    padding: "6px 10px",
+                    borderRadius: 999,
+                    background: "#F2F2F7",
+                    border: "1px solid #e6e6e6",
+                    fontSize: 12,
+                    opacity: 0.92,
+                  }}
+                >
+                  {it.conceptTag.replace(/_/g, " ")}
+                </div>
+              ))}
+            </div>
+
+            <div style={{ display: "flex", gap: 10 }}>
+              <button
+                onClick={() => void beginSuggestedReview()}
+                disabled={loading}
+                style={{
+                  padding: "10px 14px",
+                  borderRadius: 12,
+                  border: "1px solid",
+                  borderColor: loading ? "#ddd" : "#007AFF",
+                  background: loading ? "#E5E5EA" : "#007AFF",
+                  color: loading ? "#666" : "white",
+                  cursor: loading ? "not-allowed" : "pointer",
+                }}
+              >
+                Review now
+              </button>
+              <button
+                onClick={() => setReviewDismissed(true)}
+                disabled={loading}
+                style={{
+                  padding: "10px 14px",
+                  borderRadius: 12,
+                  border: "1px solid #ddd",
+                  background: "white",
+                  cursor: loading ? "not-allowed" : "pointer",
+                  opacity: loading ? 0.7 : 1,
+                }}
+              >
+                Not now
+              </button>
+            </div>
+          </div>
+        )}
+
+      {reviewBanner && (
+        <div
+          style={{
+            marginBottom: 12,
+            padding: 12,
+            borderRadius: 16,
+            border: "1px solid #e6e6e6",
+            background: "white",
+            boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+          }}
+        >
+          <div style={{ fontSize: 13, opacity: 0.85 }}>{reviewBanner}</div
+          >
+        </div
+        >
+      )}
+
+      {session && !lessonCompleted && !practiceActive && suggestedReviewItems.length > 0 && !reviewDismissed && (
+        <div
+          style={{
+            marginBottom: 12,
+            padding: 12,
+            borderRadius: 16,
+            border: "1px solid #e6e6e6",
+            background: "white",
+            boxShadow: "0 1px 2px rgba(0,0,0,0.06)",
+          }}
+        >
+          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}>
+            <div style={{ fontSize: 12, opacity: 0.75 }}>Suggested review</div>
+            <div style={{ fontSize: 12, opacity: 0.7 }}>Optional</div>
+          </div>
+
+          <div style={{ fontSize: 13, opacity: 0.85, marginBottom: 10 }}>
+            Want to quickly review {Math.min(2, suggestedReviewItems.length)} thing(s) you struggled with last time?
+          </div>
+
+          <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 12 }}>
+            {suggestedReviewItems.slice(0, 2).map((it) => (
+              <div
+                key={it.conceptTag}
+                style={{
+                  padding: "6px 10px",
+                  borderRadius: 999,
+                  background: "#F2F2F7",
+                  border: "1px solid #e6e6e6",
+                  fontSize: 12,
+                  opacity: 0.92,
+                }}
+              >
+                {it.conceptTag.replace(/_/g, " " )}
+              </div>
+            ))}
+          </div>
+
+          <div style={{ display: "flex", gap: 10 }}>
+            <button
+              onClick={() => {
+                void beginSuggestedReview();
+              }}
+              disabled={loading}
+              style={{
+                padding: "10px 12px",
+                borderRadius: 12,
+                border: "1px solid #ddd",
+                borderColor: loading ? "#ddd" : "#007AFF",
+                background: loading ? "#E5E5EA" : "#007AFF",
+                opacity: loading ? 0.7 : 1,
+                color: loading ? "#666" : "white",
+                cursor: loading ? "not-allowed" : "pointer",
+              }}
+            >
+              Review now
+            </button>
+
+            <button
+              onClick={() => setReviewDismissed(true)}
+              disabled={loading}
+              style={{
+                padding: "10px 12px",
+                borderRadius: 12,
+                border: "1px solid #ddd",
+                background: loading ? "#f6f6f6" : "white",
+                opacity: loading ? 0.7 : 1,
+                cursor: loading ? "not-allowed" : "pointer",
+              }}
+            >
+              Not now
+            </button>
+          </div>
+        </div
+        >
+      )}
+
       {/* Chat */}
       <div
         ref={chatRef}
@@ -1100,8 +1720,37 @@
             }}
           >
             <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }}>
-              <div style={{ fontSize: 12, opacity: 0.75 }}>Practice</div>
-              <div style={{ fontSize: 12, opacity: 0.7 }}>Complete this to continue</div>
+              <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
+                <div style={{ fontSize: 12, opacity: 0.75 }}>
+                  {practiceMode === "review"
+                    ? `Review ${reviewIndex + 1}/${Math.max(1, reviewQueue.length)}`
+                    : "Practice"}
+                </div>
+
+                {practiceMode === "review" && (
+                  <button
+                    onClick={() =>
+                      stopReview("Review paused. Continue the lesson when you're ready.")
+                    }
+                    disabled={loading}
+                    style={{
+                      padding: "6px 10px",
+                      borderRadius: 999,
+                      border: "1px solid #ddd",
+                      background: "white",
+                      cursor: loading ? "not-allowed" : "pointer",
+                      fontSize: 12,
+                      opacity: loading ? 0.7 : 1,
+                    }}
+                  >
+                    Stop
+                  </button>
+                )}
+              </div>
+
+              <div style={{ fontSize: 12, opacity: 0.7 }}>
+                {practiceMode === "review" ? "Optional review" : "Complete this to continue"}
+              </div>
             </div>
             <div style={{ whiteSpace: "pre-wrap", marginBottom: 10 }}>{practicePrompt}</div>
 
